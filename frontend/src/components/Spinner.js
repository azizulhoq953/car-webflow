// src/components/Spinner.js
import React from 'react';
import './Spinner.css'; // Optional: for custom spinner styling

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
